// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"recruit/ent/employeedesignation"
	"recruit/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// EmployeeDesignationQuery is the builder for querying EmployeeDesignation entities.
type EmployeeDesignationQuery struct {
	config
	ctx        *QueryContext
	order      []employeedesignation.OrderOption
	inters     []Interceptor
	predicates []predicate.EmployeeDesignation
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EmployeeDesignationQuery builder.
func (edq *EmployeeDesignationQuery) Where(ps ...predicate.EmployeeDesignation) *EmployeeDesignationQuery {
	edq.predicates = append(edq.predicates, ps...)
	return edq
}

// Limit the number of records to be returned by this query.
func (edq *EmployeeDesignationQuery) Limit(limit int) *EmployeeDesignationQuery {
	edq.ctx.Limit = &limit
	return edq
}

// Offset to start from.
func (edq *EmployeeDesignationQuery) Offset(offset int) *EmployeeDesignationQuery {
	edq.ctx.Offset = &offset
	return edq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (edq *EmployeeDesignationQuery) Unique(unique bool) *EmployeeDesignationQuery {
	edq.ctx.Unique = &unique
	return edq
}

// Order specifies how the records should be ordered.
func (edq *EmployeeDesignationQuery) Order(o ...employeedesignation.OrderOption) *EmployeeDesignationQuery {
	edq.order = append(edq.order, o...)
	return edq
}

// First returns the first EmployeeDesignation entity from the query.
// Returns a *NotFoundError when no EmployeeDesignation was found.
func (edq *EmployeeDesignationQuery) First(ctx context.Context) (*EmployeeDesignation, error) {
	nodes, err := edq.Limit(1).All(setContextOp(ctx, edq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{employeedesignation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (edq *EmployeeDesignationQuery) FirstX(ctx context.Context) *EmployeeDesignation {
	node, err := edq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EmployeeDesignation ID from the query.
// Returns a *NotFoundError when no EmployeeDesignation ID was found.
func (edq *EmployeeDesignationQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = edq.Limit(1).IDs(setContextOp(ctx, edq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{employeedesignation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (edq *EmployeeDesignationQuery) FirstIDX(ctx context.Context) int32 {
	id, err := edq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EmployeeDesignation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EmployeeDesignation entity is found.
// Returns a *NotFoundError when no EmployeeDesignation entities are found.
func (edq *EmployeeDesignationQuery) Only(ctx context.Context) (*EmployeeDesignation, error) {
	nodes, err := edq.Limit(2).All(setContextOp(ctx, edq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{employeedesignation.Label}
	default:
		return nil, &NotSingularError{employeedesignation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (edq *EmployeeDesignationQuery) OnlyX(ctx context.Context) *EmployeeDesignation {
	node, err := edq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EmployeeDesignation ID in the query.
// Returns a *NotSingularError when more than one EmployeeDesignation ID is found.
// Returns a *NotFoundError when no entities are found.
func (edq *EmployeeDesignationQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = edq.Limit(2).IDs(setContextOp(ctx, edq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{employeedesignation.Label}
	default:
		err = &NotSingularError{employeedesignation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (edq *EmployeeDesignationQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := edq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EmployeeDesignations.
func (edq *EmployeeDesignationQuery) All(ctx context.Context) ([]*EmployeeDesignation, error) {
	ctx = setContextOp(ctx, edq.ctx, "All")
	if err := edq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EmployeeDesignation, *EmployeeDesignationQuery]()
	return withInterceptors[[]*EmployeeDesignation](ctx, edq, qr, edq.inters)
}

// AllX is like All, but panics if an error occurs.
func (edq *EmployeeDesignationQuery) AllX(ctx context.Context) []*EmployeeDesignation {
	nodes, err := edq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EmployeeDesignation IDs.
func (edq *EmployeeDesignationQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if edq.ctx.Unique == nil && edq.path != nil {
		edq.Unique(true)
	}
	ctx = setContextOp(ctx, edq.ctx, "IDs")
	if err = edq.Select(employeedesignation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (edq *EmployeeDesignationQuery) IDsX(ctx context.Context) []int32 {
	ids, err := edq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (edq *EmployeeDesignationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, edq.ctx, "Count")
	if err := edq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, edq, querierCount[*EmployeeDesignationQuery](), edq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (edq *EmployeeDesignationQuery) CountX(ctx context.Context) int {
	count, err := edq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (edq *EmployeeDesignationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, edq.ctx, "Exist")
	switch _, err := edq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (edq *EmployeeDesignationQuery) ExistX(ctx context.Context) bool {
	exist, err := edq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EmployeeDesignationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (edq *EmployeeDesignationQuery) Clone() *EmployeeDesignationQuery {
	if edq == nil {
		return nil
	}
	return &EmployeeDesignationQuery{
		config:     edq.config,
		ctx:        edq.ctx.Clone(),
		order:      append([]employeedesignation.OrderOption{}, edq.order...),
		inters:     append([]Interceptor{}, edq.inters...),
		predicates: append([]predicate.EmployeeDesignation{}, edq.predicates...),
		// clone intermediate query.
		sql:  edq.sql.Clone(),
		path: edq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PostID int32 `json:"PostID,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EmployeeDesignation.Query().
//		GroupBy(employeedesignation.FieldPostID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (edq *EmployeeDesignationQuery) GroupBy(field string, fields ...string) *EmployeeDesignationGroupBy {
	edq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EmployeeDesignationGroupBy{build: edq}
	grbuild.flds = &edq.ctx.Fields
	grbuild.label = employeedesignation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PostID int32 `json:"PostID,omitempty"`
//	}
//
//	client.EmployeeDesignation.Query().
//		Select(employeedesignation.FieldPostID).
//		Scan(ctx, &v)
func (edq *EmployeeDesignationQuery) Select(fields ...string) *EmployeeDesignationSelect {
	edq.ctx.Fields = append(edq.ctx.Fields, fields...)
	sbuild := &EmployeeDesignationSelect{EmployeeDesignationQuery: edq}
	sbuild.label = employeedesignation.Label
	sbuild.flds, sbuild.scan = &edq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EmployeeDesignationSelect configured with the given aggregations.
func (edq *EmployeeDesignationQuery) Aggregate(fns ...AggregateFunc) *EmployeeDesignationSelect {
	return edq.Select().Aggregate(fns...)
}

func (edq *EmployeeDesignationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range edq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, edq); err != nil {
				return err
			}
		}
	}
	for _, f := range edq.ctx.Fields {
		if !employeedesignation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if edq.path != nil {
		prev, err := edq.path(ctx)
		if err != nil {
			return err
		}
		edq.sql = prev
	}
	return nil
}

func (edq *EmployeeDesignationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EmployeeDesignation, error) {
	var (
		nodes = []*EmployeeDesignation{}
		_spec = edq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EmployeeDesignation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EmployeeDesignation{config: edq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, edq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (edq *EmployeeDesignationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := edq.querySpec()
	_spec.Node.Columns = edq.ctx.Fields
	if len(edq.ctx.Fields) > 0 {
		_spec.Unique = edq.ctx.Unique != nil && *edq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, edq.driver, _spec)
}

func (edq *EmployeeDesignationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(employeedesignation.Table, employeedesignation.Columns, sqlgraph.NewFieldSpec(employeedesignation.FieldID, field.TypeInt32))
	_spec.From = edq.sql
	if unique := edq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if edq.path != nil {
		_spec.Unique = true
	}
	if fields := edq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, employeedesignation.FieldID)
		for i := range fields {
			if fields[i] != employeedesignation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := edq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := edq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := edq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := edq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (edq *EmployeeDesignationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(edq.driver.Dialect())
	t1 := builder.Table(employeedesignation.Table)
	columns := edq.ctx.Fields
	if len(columns) == 0 {
		columns = employeedesignation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if edq.sql != nil {
		selector = edq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if edq.ctx.Unique != nil && *edq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range edq.predicates {
		p(selector)
	}
	for _, p := range edq.order {
		p(selector)
	}
	if offset := edq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := edq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EmployeeDesignationGroupBy is the group-by builder for EmployeeDesignation entities.
type EmployeeDesignationGroupBy struct {
	selector
	build *EmployeeDesignationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (edgb *EmployeeDesignationGroupBy) Aggregate(fns ...AggregateFunc) *EmployeeDesignationGroupBy {
	edgb.fns = append(edgb.fns, fns...)
	return edgb
}

// Scan applies the selector query and scans the result into the given value.
func (edgb *EmployeeDesignationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, edgb.build.ctx, "GroupBy")
	if err := edgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmployeeDesignationQuery, *EmployeeDesignationGroupBy](ctx, edgb.build, edgb, edgb.build.inters, v)
}

func (edgb *EmployeeDesignationGroupBy) sqlScan(ctx context.Context, root *EmployeeDesignationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(edgb.fns))
	for _, fn := range edgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*edgb.flds)+len(edgb.fns))
		for _, f := range *edgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*edgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := edgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EmployeeDesignationSelect is the builder for selecting fields of EmployeeDesignation entities.
type EmployeeDesignationSelect struct {
	*EmployeeDesignationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (eds *EmployeeDesignationSelect) Aggregate(fns ...AggregateFunc) *EmployeeDesignationSelect {
	eds.fns = append(eds.fns, fns...)
	return eds
}

// Scan applies the selector query and scans the result into the given value.
func (eds *EmployeeDesignationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eds.ctx, "Select")
	if err := eds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EmployeeDesignationQuery, *EmployeeDesignationSelect](ctx, eds.EmployeeDesignationQuery, eds, eds.inters, v)
}

func (eds *EmployeeDesignationSelect) sqlScan(ctx context.Context, root *EmployeeDesignationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(eds.fns))
	for _, fn := range eds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*eds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
