// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"recruit/ent/exampostmapping"
	"recruit/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExamPostMappingQuery is the builder for querying ExamPostMapping entities.
type ExamPostMappingQuery struct {
	config
	ctx        *QueryContext
	order      []exampostmapping.OrderOption
	inters     []Interceptor
	predicates []predicate.ExamPostMapping
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExamPostMappingQuery builder.
func (epmq *ExamPostMappingQuery) Where(ps ...predicate.ExamPostMapping) *ExamPostMappingQuery {
	epmq.predicates = append(epmq.predicates, ps...)
	return epmq
}

// Limit the number of records to be returned by this query.
func (epmq *ExamPostMappingQuery) Limit(limit int) *ExamPostMappingQuery {
	epmq.ctx.Limit = &limit
	return epmq
}

// Offset to start from.
func (epmq *ExamPostMappingQuery) Offset(offset int) *ExamPostMappingQuery {
	epmq.ctx.Offset = &offset
	return epmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (epmq *ExamPostMappingQuery) Unique(unique bool) *ExamPostMappingQuery {
	epmq.ctx.Unique = &unique
	return epmq
}

// Order specifies how the records should be ordered.
func (epmq *ExamPostMappingQuery) Order(o ...exampostmapping.OrderOption) *ExamPostMappingQuery {
	epmq.order = append(epmq.order, o...)
	return epmq
}

// First returns the first ExamPostMapping entity from the query.
// Returns a *NotFoundError when no ExamPostMapping was found.
func (epmq *ExamPostMappingQuery) First(ctx context.Context) (*ExamPostMapping, error) {
	nodes, err := epmq.Limit(1).All(setContextOp(ctx, epmq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{exampostmapping.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (epmq *ExamPostMappingQuery) FirstX(ctx context.Context) *ExamPostMapping {
	node, err := epmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ExamPostMapping ID from the query.
// Returns a *NotFoundError when no ExamPostMapping ID was found.
func (epmq *ExamPostMappingQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = epmq.Limit(1).IDs(setContextOp(ctx, epmq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{exampostmapping.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (epmq *ExamPostMappingQuery) FirstIDX(ctx context.Context) int {
	id, err := epmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ExamPostMapping entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ExamPostMapping entity is found.
// Returns a *NotFoundError when no ExamPostMapping entities are found.
func (epmq *ExamPostMappingQuery) Only(ctx context.Context) (*ExamPostMapping, error) {
	nodes, err := epmq.Limit(2).All(setContextOp(ctx, epmq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{exampostmapping.Label}
	default:
		return nil, &NotSingularError{exampostmapping.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (epmq *ExamPostMappingQuery) OnlyX(ctx context.Context) *ExamPostMapping {
	node, err := epmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ExamPostMapping ID in the query.
// Returns a *NotSingularError when more than one ExamPostMapping ID is found.
// Returns a *NotFoundError when no entities are found.
func (epmq *ExamPostMappingQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = epmq.Limit(2).IDs(setContextOp(ctx, epmq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{exampostmapping.Label}
	default:
		err = &NotSingularError{exampostmapping.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (epmq *ExamPostMappingQuery) OnlyIDX(ctx context.Context) int {
	id, err := epmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ExamPostMappings.
func (epmq *ExamPostMappingQuery) All(ctx context.Context) ([]*ExamPostMapping, error) {
	ctx = setContextOp(ctx, epmq.ctx, "All")
	if err := epmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ExamPostMapping, *ExamPostMappingQuery]()
	return withInterceptors[[]*ExamPostMapping](ctx, epmq, qr, epmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (epmq *ExamPostMappingQuery) AllX(ctx context.Context) []*ExamPostMapping {
	nodes, err := epmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ExamPostMapping IDs.
func (epmq *ExamPostMappingQuery) IDs(ctx context.Context) (ids []int, err error) {
	if epmq.ctx.Unique == nil && epmq.path != nil {
		epmq.Unique(true)
	}
	ctx = setContextOp(ctx, epmq.ctx, "IDs")
	if err = epmq.Select(exampostmapping.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (epmq *ExamPostMappingQuery) IDsX(ctx context.Context) []int {
	ids, err := epmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (epmq *ExamPostMappingQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, epmq.ctx, "Count")
	if err := epmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, epmq, querierCount[*ExamPostMappingQuery](), epmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (epmq *ExamPostMappingQuery) CountX(ctx context.Context) int {
	count, err := epmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (epmq *ExamPostMappingQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, epmq.ctx, "Exist")
	switch _, err := epmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (epmq *ExamPostMappingQuery) ExistX(ctx context.Context) bool {
	exist, err := epmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExamPostMappingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (epmq *ExamPostMappingQuery) Clone() *ExamPostMappingQuery {
	if epmq == nil {
		return nil
	}
	return &ExamPostMappingQuery{
		config:     epmq.config,
		ctx:        epmq.ctx.Clone(),
		order:      append([]exampostmapping.OrderOption{}, epmq.order...),
		inters:     append([]Interceptor{}, epmq.inters...),
		predicates: append([]predicate.ExamPostMapping{}, epmq.predicates...),
		// clone intermediate query.
		sql:  epmq.sql.Clone(),
		path: epmq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ExamCode int64 `json:"ExamCode,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ExamPostMapping.Query().
//		GroupBy(exampostmapping.FieldExamCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (epmq *ExamPostMappingQuery) GroupBy(field string, fields ...string) *ExamPostMappingGroupBy {
	epmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExamPostMappingGroupBy{build: epmq}
	grbuild.flds = &epmq.ctx.Fields
	grbuild.label = exampostmapping.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ExamCode int64 `json:"ExamCode,omitempty"`
//	}
//
//	client.ExamPostMapping.Query().
//		Select(exampostmapping.FieldExamCode).
//		Scan(ctx, &v)
func (epmq *ExamPostMappingQuery) Select(fields ...string) *ExamPostMappingSelect {
	epmq.ctx.Fields = append(epmq.ctx.Fields, fields...)
	sbuild := &ExamPostMappingSelect{ExamPostMappingQuery: epmq}
	sbuild.label = exampostmapping.Label
	sbuild.flds, sbuild.scan = &epmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExamPostMappingSelect configured with the given aggregations.
func (epmq *ExamPostMappingQuery) Aggregate(fns ...AggregateFunc) *ExamPostMappingSelect {
	return epmq.Select().Aggregate(fns...)
}

func (epmq *ExamPostMappingQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range epmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, epmq); err != nil {
				return err
			}
		}
	}
	for _, f := range epmq.ctx.Fields {
		if !exampostmapping.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if epmq.path != nil {
		prev, err := epmq.path(ctx)
		if err != nil {
			return err
		}
		epmq.sql = prev
	}
	return nil
}

func (epmq *ExamPostMappingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ExamPostMapping, error) {
	var (
		nodes = []*ExamPostMapping{}
		_spec = epmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ExamPostMapping).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ExamPostMapping{config: epmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, epmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (epmq *ExamPostMappingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := epmq.querySpec()
	_spec.Node.Columns = epmq.ctx.Fields
	if len(epmq.ctx.Fields) > 0 {
		_spec.Unique = epmq.ctx.Unique != nil && *epmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, epmq.driver, _spec)
}

func (epmq *ExamPostMappingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(exampostmapping.Table, exampostmapping.Columns, sqlgraph.NewFieldSpec(exampostmapping.FieldID, field.TypeInt))
	_spec.From = epmq.sql
	if unique := epmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if epmq.path != nil {
		_spec.Unique = true
	}
	if fields := epmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, exampostmapping.FieldID)
		for i := range fields {
			if fields[i] != exampostmapping.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := epmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := epmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := epmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := epmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (epmq *ExamPostMappingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(epmq.driver.Dialect())
	t1 := builder.Table(exampostmapping.Table)
	columns := epmq.ctx.Fields
	if len(columns) == 0 {
		columns = exampostmapping.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if epmq.sql != nil {
		selector = epmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if epmq.ctx.Unique != nil && *epmq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range epmq.predicates {
		p(selector)
	}
	for _, p := range epmq.order {
		p(selector)
	}
	if offset := epmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := epmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ExamPostMappingGroupBy is the group-by builder for ExamPostMapping entities.
type ExamPostMappingGroupBy struct {
	selector
	build *ExamPostMappingQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (epmgb *ExamPostMappingGroupBy) Aggregate(fns ...AggregateFunc) *ExamPostMappingGroupBy {
	epmgb.fns = append(epmgb.fns, fns...)
	return epmgb
}

// Scan applies the selector query and scans the result into the given value.
func (epmgb *ExamPostMappingGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, epmgb.build.ctx, "GroupBy")
	if err := epmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamPostMappingQuery, *ExamPostMappingGroupBy](ctx, epmgb.build, epmgb, epmgb.build.inters, v)
}

func (epmgb *ExamPostMappingGroupBy) sqlScan(ctx context.Context, root *ExamPostMappingQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(epmgb.fns))
	for _, fn := range epmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*epmgb.flds)+len(epmgb.fns))
		for _, f := range *epmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*epmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := epmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExamPostMappingSelect is the builder for selecting fields of ExamPostMapping entities.
type ExamPostMappingSelect struct {
	*ExamPostMappingQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (epms *ExamPostMappingSelect) Aggregate(fns ...AggregateFunc) *ExamPostMappingSelect {
	epms.fns = append(epms.fns, fns...)
	return epms
}

// Scan applies the selector query and scans the result into the given value.
func (epms *ExamPostMappingSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, epms.ctx, "Select")
	if err := epms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamPostMappingQuery, *ExamPostMappingSelect](ctx, epms.ExamPostMappingQuery, epms, epms.inters, v)
}

func (epms *ExamPostMappingSelect) sqlScan(ctx context.Context, root *ExamPostMappingQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(epms.fns))
	for _, fn := range epms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*epms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := epms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
