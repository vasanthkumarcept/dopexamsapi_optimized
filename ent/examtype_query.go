// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"recruit/ent/exam"
	"recruit/ent/examtype"
	"recruit/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExamTypeQuery is the builder for querying ExamType entities.
type ExamTypeQuery struct {
	config
	ctx        *QueryContext
	order      []examtype.OrderOption
	inters     []Interceptor
	predicates []predicate.ExamType
	withExam   *ExamQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExamTypeQuery builder.
func (etq *ExamTypeQuery) Where(ps ...predicate.ExamType) *ExamTypeQuery {
	etq.predicates = append(etq.predicates, ps...)
	return etq
}

// Limit the number of records to be returned by this query.
func (etq *ExamTypeQuery) Limit(limit int) *ExamTypeQuery {
	etq.ctx.Limit = &limit
	return etq
}

// Offset to start from.
func (etq *ExamTypeQuery) Offset(offset int) *ExamTypeQuery {
	etq.ctx.Offset = &offset
	return etq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (etq *ExamTypeQuery) Unique(unique bool) *ExamTypeQuery {
	etq.ctx.Unique = &unique
	return etq
}

// Order specifies how the records should be ordered.
func (etq *ExamTypeQuery) Order(o ...examtype.OrderOption) *ExamTypeQuery {
	etq.order = append(etq.order, o...)
	return etq
}

// QueryExam chains the current query on the "exam" edge.
func (etq *ExamTypeQuery) QueryExam() *ExamQuery {
	query := (&ExamClient{config: etq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := etq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := etq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(examtype.Table, examtype.FieldID, selector),
			sqlgraph.To(exam.Table, exam.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, examtype.ExamTable, examtype.ExamColumn),
		)
		fromU = sqlgraph.SetNeighbors(etq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ExamType entity from the query.
// Returns a *NotFoundError when no ExamType was found.
func (etq *ExamTypeQuery) First(ctx context.Context) (*ExamType, error) {
	nodes, err := etq.Limit(1).All(setContextOp(ctx, etq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{examtype.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (etq *ExamTypeQuery) FirstX(ctx context.Context) *ExamType {
	node, err := etq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ExamType ID from the query.
// Returns a *NotFoundError when no ExamType ID was found.
func (etq *ExamTypeQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = etq.Limit(1).IDs(setContextOp(ctx, etq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{examtype.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (etq *ExamTypeQuery) FirstIDX(ctx context.Context) int32 {
	id, err := etq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ExamType entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ExamType entity is found.
// Returns a *NotFoundError when no ExamType entities are found.
func (etq *ExamTypeQuery) Only(ctx context.Context) (*ExamType, error) {
	nodes, err := etq.Limit(2).All(setContextOp(ctx, etq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{examtype.Label}
	default:
		return nil, &NotSingularError{examtype.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (etq *ExamTypeQuery) OnlyX(ctx context.Context) *ExamType {
	node, err := etq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ExamType ID in the query.
// Returns a *NotSingularError when more than one ExamType ID is found.
// Returns a *NotFoundError when no entities are found.
func (etq *ExamTypeQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = etq.Limit(2).IDs(setContextOp(ctx, etq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{examtype.Label}
	default:
		err = &NotSingularError{examtype.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (etq *ExamTypeQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := etq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ExamTypes.
func (etq *ExamTypeQuery) All(ctx context.Context) ([]*ExamType, error) {
	ctx = setContextOp(ctx, etq.ctx, "All")
	if err := etq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ExamType, *ExamTypeQuery]()
	return withInterceptors[[]*ExamType](ctx, etq, qr, etq.inters)
}

// AllX is like All, but panics if an error occurs.
func (etq *ExamTypeQuery) AllX(ctx context.Context) []*ExamType {
	nodes, err := etq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ExamType IDs.
func (etq *ExamTypeQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if etq.ctx.Unique == nil && etq.path != nil {
		etq.Unique(true)
	}
	ctx = setContextOp(ctx, etq.ctx, "IDs")
	if err = etq.Select(examtype.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (etq *ExamTypeQuery) IDsX(ctx context.Context) []int32 {
	ids, err := etq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (etq *ExamTypeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, etq.ctx, "Count")
	if err := etq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, etq, querierCount[*ExamTypeQuery](), etq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (etq *ExamTypeQuery) CountX(ctx context.Context) int {
	count, err := etq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (etq *ExamTypeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, etq.ctx, "Exist")
	switch _, err := etq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (etq *ExamTypeQuery) ExistX(ctx context.Context) bool {
	exist, err := etq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExamTypeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (etq *ExamTypeQuery) Clone() *ExamTypeQuery {
	if etq == nil {
		return nil
	}
	return &ExamTypeQuery{
		config:     etq.config,
		ctx:        etq.ctx.Clone(),
		order:      append([]examtype.OrderOption{}, etq.order...),
		inters:     append([]Interceptor{}, etq.inters...),
		predicates: append([]predicate.ExamType{}, etq.predicates...),
		withExam:   etq.withExam.Clone(),
		// clone intermediate query.
		sql:  etq.sql.Clone(),
		path: etq.path,
	}
}

// WithExam tells the query-builder to eager-load the nodes that are connected to
// the "exam" edge. The optional arguments are used to configure the query builder of the edge.
func (etq *ExamTypeQuery) WithExam(opts ...func(*ExamQuery)) *ExamTypeQuery {
	query := (&ExamClient{config: etq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	etq.withExam = query
	return etq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ExamType string `json:"ExamType,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ExamType.Query().
//		GroupBy(examtype.FieldExamType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (etq *ExamTypeQuery) GroupBy(field string, fields ...string) *ExamTypeGroupBy {
	etq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExamTypeGroupBy{build: etq}
	grbuild.flds = &etq.ctx.Fields
	grbuild.label = examtype.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ExamType string `json:"ExamType,omitempty"`
//	}
//
//	client.ExamType.Query().
//		Select(examtype.FieldExamType).
//		Scan(ctx, &v)
func (etq *ExamTypeQuery) Select(fields ...string) *ExamTypeSelect {
	etq.ctx.Fields = append(etq.ctx.Fields, fields...)
	sbuild := &ExamTypeSelect{ExamTypeQuery: etq}
	sbuild.label = examtype.Label
	sbuild.flds, sbuild.scan = &etq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExamTypeSelect configured with the given aggregations.
func (etq *ExamTypeQuery) Aggregate(fns ...AggregateFunc) *ExamTypeSelect {
	return etq.Select().Aggregate(fns...)
}

func (etq *ExamTypeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range etq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, etq); err != nil {
				return err
			}
		}
	}
	for _, f := range etq.ctx.Fields {
		if !examtype.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if etq.path != nil {
		prev, err := etq.path(ctx)
		if err != nil {
			return err
		}
		etq.sql = prev
	}
	return nil
}

func (etq *ExamTypeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ExamType, error) {
	var (
		nodes       = []*ExamType{}
		_spec       = etq.querySpec()
		loadedTypes = [1]bool{
			etq.withExam != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ExamType).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ExamType{config: etq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, etq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := etq.withExam; query != nil {
		if err := etq.loadExam(ctx, query, nodes, nil,
			func(n *ExamType, e *Exam) { n.Edges.Exam = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (etq *ExamTypeQuery) loadExam(ctx context.Context, query *ExamQuery, nodes []*ExamType, init func(*ExamType), assign func(*ExamType, *Exam)) error {
	ids := make([]int32, 0, len(nodes))
	nodeids := make(map[int32][]*ExamType)
	for i := range nodes {
		fk := nodes[i].ExamCode
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(exam.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "ExamCode" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (etq *ExamTypeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := etq.querySpec()
	_spec.Node.Columns = etq.ctx.Fields
	if len(etq.ctx.Fields) > 0 {
		_spec.Unique = etq.ctx.Unique != nil && *etq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, etq.driver, _spec)
}

func (etq *ExamTypeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(examtype.Table, examtype.Columns, sqlgraph.NewFieldSpec(examtype.FieldID, field.TypeInt32))
	_spec.From = etq.sql
	if unique := etq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if etq.path != nil {
		_spec.Unique = true
	}
	if fields := etq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, examtype.FieldID)
		for i := range fields {
			if fields[i] != examtype.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if etq.withExam != nil {
			_spec.Node.AddColumnOnce(examtype.FieldExamCode)
		}
	}
	if ps := etq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := etq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := etq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := etq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (etq *ExamTypeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(etq.driver.Dialect())
	t1 := builder.Table(examtype.Table)
	columns := etq.ctx.Fields
	if len(columns) == 0 {
		columns = examtype.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if etq.sql != nil {
		selector = etq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if etq.ctx.Unique != nil && *etq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range etq.predicates {
		p(selector)
	}
	for _, p := range etq.order {
		p(selector)
	}
	if offset := etq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := etq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ExamTypeGroupBy is the group-by builder for ExamType entities.
type ExamTypeGroupBy struct {
	selector
	build *ExamTypeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (etgb *ExamTypeGroupBy) Aggregate(fns ...AggregateFunc) *ExamTypeGroupBy {
	etgb.fns = append(etgb.fns, fns...)
	return etgb
}

// Scan applies the selector query and scans the result into the given value.
func (etgb *ExamTypeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, etgb.build.ctx, "GroupBy")
	if err := etgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamTypeQuery, *ExamTypeGroupBy](ctx, etgb.build, etgb, etgb.build.inters, v)
}

func (etgb *ExamTypeGroupBy) sqlScan(ctx context.Context, root *ExamTypeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(etgb.fns))
	for _, fn := range etgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*etgb.flds)+len(etgb.fns))
		for _, f := range *etgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*etgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := etgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExamTypeSelect is the builder for selecting fields of ExamType entities.
type ExamTypeSelect struct {
	*ExamTypeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ets *ExamTypeSelect) Aggregate(fns ...AggregateFunc) *ExamTypeSelect {
	ets.fns = append(ets.fns, fns...)
	return ets
}

// Scan applies the selector query and scans the result into the given value.
func (ets *ExamTypeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ets.ctx, "Select")
	if err := ets.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamTypeQuery, *ExamTypeSelect](ctx, ets.ExamTypeQuery, ets, ets.inters, v)
}

func (ets *ExamTypeSelect) sqlScan(ctx context.Context, root *ExamTypeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ets.fns))
	for _, fn := range ets.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ets.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ets.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
