// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"recruit/ent/categorymininummarkmapping"
	"recruit/ent/logs"
	"recruit/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CategoryMininumMarkMappingQuery is the builder for querying CategoryMininumMarkMapping entities.
type CategoryMininumMarkMappingQuery struct {
	config
	ctx         *QueryContext
	order       []categorymininummarkmapping.OrderOption
	inters      []Interceptor
	predicates  []predicate.CategoryMininumMarkMapping
	withLogData *LogsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CategoryMininumMarkMappingQuery builder.
func (cmmmq *CategoryMininumMarkMappingQuery) Where(ps ...predicate.CategoryMininumMarkMapping) *CategoryMininumMarkMappingQuery {
	cmmmq.predicates = append(cmmmq.predicates, ps...)
	return cmmmq
}

// Limit the number of records to be returned by this query.
func (cmmmq *CategoryMininumMarkMappingQuery) Limit(limit int) *CategoryMininumMarkMappingQuery {
	cmmmq.ctx.Limit = &limit
	return cmmmq
}

// Offset to start from.
func (cmmmq *CategoryMininumMarkMappingQuery) Offset(offset int) *CategoryMininumMarkMappingQuery {
	cmmmq.ctx.Offset = &offset
	return cmmmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cmmmq *CategoryMininumMarkMappingQuery) Unique(unique bool) *CategoryMininumMarkMappingQuery {
	cmmmq.ctx.Unique = &unique
	return cmmmq
}

// Order specifies how the records should be ordered.
func (cmmmq *CategoryMininumMarkMappingQuery) Order(o ...categorymininummarkmapping.OrderOption) *CategoryMininumMarkMappingQuery {
	cmmmq.order = append(cmmmq.order, o...)
	return cmmmq
}

// QueryLogData chains the current query on the "LogData" edge.
func (cmmmq *CategoryMininumMarkMappingQuery) QueryLogData() *LogsQuery {
	query := (&LogsClient{config: cmmmq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cmmmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cmmmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(categorymininummarkmapping.Table, categorymininummarkmapping.FieldID, selector),
			sqlgraph.To(logs.Table, logs.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, categorymininummarkmapping.LogDataTable, categorymininummarkmapping.LogDataColumn),
		)
		fromU = sqlgraph.SetNeighbors(cmmmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CategoryMininumMarkMapping entity from the query.
// Returns a *NotFoundError when no CategoryMininumMarkMapping was found.
func (cmmmq *CategoryMininumMarkMappingQuery) First(ctx context.Context) (*CategoryMininumMarkMapping, error) {
	nodes, err := cmmmq.Limit(1).All(setContextOp(ctx, cmmmq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{categorymininummarkmapping.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cmmmq *CategoryMininumMarkMappingQuery) FirstX(ctx context.Context) *CategoryMininumMarkMapping {
	node, err := cmmmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CategoryMininumMarkMapping ID from the query.
// Returns a *NotFoundError when no CategoryMininumMarkMapping ID was found.
func (cmmmq *CategoryMininumMarkMappingQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cmmmq.Limit(1).IDs(setContextOp(ctx, cmmmq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{categorymininummarkmapping.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cmmmq *CategoryMininumMarkMappingQuery) FirstIDX(ctx context.Context) int64 {
	id, err := cmmmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CategoryMininumMarkMapping entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CategoryMininumMarkMapping entity is found.
// Returns a *NotFoundError when no CategoryMininumMarkMapping entities are found.
func (cmmmq *CategoryMininumMarkMappingQuery) Only(ctx context.Context) (*CategoryMininumMarkMapping, error) {
	nodes, err := cmmmq.Limit(2).All(setContextOp(ctx, cmmmq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{categorymininummarkmapping.Label}
	default:
		return nil, &NotSingularError{categorymininummarkmapping.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cmmmq *CategoryMininumMarkMappingQuery) OnlyX(ctx context.Context) *CategoryMininumMarkMapping {
	node, err := cmmmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CategoryMininumMarkMapping ID in the query.
// Returns a *NotSingularError when more than one CategoryMininumMarkMapping ID is found.
// Returns a *NotFoundError when no entities are found.
func (cmmmq *CategoryMininumMarkMappingQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cmmmq.Limit(2).IDs(setContextOp(ctx, cmmmq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{categorymininummarkmapping.Label}
	default:
		err = &NotSingularError{categorymininummarkmapping.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cmmmq *CategoryMininumMarkMappingQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := cmmmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CategoryMininumMarkMappings.
func (cmmmq *CategoryMininumMarkMappingQuery) All(ctx context.Context) ([]*CategoryMininumMarkMapping, error) {
	ctx = setContextOp(ctx, cmmmq.ctx, "All")
	if err := cmmmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CategoryMininumMarkMapping, *CategoryMininumMarkMappingQuery]()
	return withInterceptors[[]*CategoryMininumMarkMapping](ctx, cmmmq, qr, cmmmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cmmmq *CategoryMininumMarkMappingQuery) AllX(ctx context.Context) []*CategoryMininumMarkMapping {
	nodes, err := cmmmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CategoryMininumMarkMapping IDs.
func (cmmmq *CategoryMininumMarkMappingQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if cmmmq.ctx.Unique == nil && cmmmq.path != nil {
		cmmmq.Unique(true)
	}
	ctx = setContextOp(ctx, cmmmq.ctx, "IDs")
	if err = cmmmq.Select(categorymininummarkmapping.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cmmmq *CategoryMininumMarkMappingQuery) IDsX(ctx context.Context) []int64 {
	ids, err := cmmmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cmmmq *CategoryMininumMarkMappingQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cmmmq.ctx, "Count")
	if err := cmmmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cmmmq, querierCount[*CategoryMininumMarkMappingQuery](), cmmmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cmmmq *CategoryMininumMarkMappingQuery) CountX(ctx context.Context) int {
	count, err := cmmmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cmmmq *CategoryMininumMarkMappingQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cmmmq.ctx, "Exist")
	switch _, err := cmmmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cmmmq *CategoryMininumMarkMappingQuery) ExistX(ctx context.Context) bool {
	exist, err := cmmmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CategoryMininumMarkMappingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cmmmq *CategoryMininumMarkMappingQuery) Clone() *CategoryMininumMarkMappingQuery {
	if cmmmq == nil {
		return nil
	}
	return &CategoryMininumMarkMappingQuery{
		config:      cmmmq.config,
		ctx:         cmmmq.ctx.Clone(),
		order:       append([]categorymininummarkmapping.OrderOption{}, cmmmq.order...),
		inters:      append([]Interceptor{}, cmmmq.inters...),
		predicates:  append([]predicate.CategoryMininumMarkMapping{}, cmmmq.predicates...),
		withLogData: cmmmq.withLogData.Clone(),
		// clone intermediate query.
		sql:  cmmmq.sql.Clone(),
		path: cmmmq.path,
	}
}

// WithLogData tells the query-builder to eager-load the nodes that are connected to
// the "LogData" edge. The optional arguments are used to configure the query builder of the edge.
func (cmmmq *CategoryMininumMarkMappingQuery) WithLogData(opts ...func(*LogsQuery)) *CategoryMininumMarkMappingQuery {
	query := (&LogsClient{config: cmmmq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cmmmq.withLogData = query
	return cmmmq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ExamCode int64 `json:"ExamCode,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CategoryMininumMarkMapping.Query().
//		GroupBy(categorymininummarkmapping.FieldExamCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cmmmq *CategoryMininumMarkMappingQuery) GroupBy(field string, fields ...string) *CategoryMininumMarkMappingGroupBy {
	cmmmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CategoryMininumMarkMappingGroupBy{build: cmmmq}
	grbuild.flds = &cmmmq.ctx.Fields
	grbuild.label = categorymininummarkmapping.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ExamCode int64 `json:"ExamCode,omitempty"`
//	}
//
//	client.CategoryMininumMarkMapping.Query().
//		Select(categorymininummarkmapping.FieldExamCode).
//		Scan(ctx, &v)
func (cmmmq *CategoryMininumMarkMappingQuery) Select(fields ...string) *CategoryMininumMarkMappingSelect {
	cmmmq.ctx.Fields = append(cmmmq.ctx.Fields, fields...)
	sbuild := &CategoryMininumMarkMappingSelect{CategoryMininumMarkMappingQuery: cmmmq}
	sbuild.label = categorymininummarkmapping.Label
	sbuild.flds, sbuild.scan = &cmmmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CategoryMininumMarkMappingSelect configured with the given aggregations.
func (cmmmq *CategoryMininumMarkMappingQuery) Aggregate(fns ...AggregateFunc) *CategoryMininumMarkMappingSelect {
	return cmmmq.Select().Aggregate(fns...)
}

func (cmmmq *CategoryMininumMarkMappingQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cmmmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cmmmq); err != nil {
				return err
			}
		}
	}
	for _, f := range cmmmq.ctx.Fields {
		if !categorymininummarkmapping.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cmmmq.path != nil {
		prev, err := cmmmq.path(ctx)
		if err != nil {
			return err
		}
		cmmmq.sql = prev
	}
	return nil
}

func (cmmmq *CategoryMininumMarkMappingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CategoryMininumMarkMapping, error) {
	var (
		nodes       = []*CategoryMininumMarkMapping{}
		_spec       = cmmmq.querySpec()
		loadedTypes = [1]bool{
			cmmmq.withLogData != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CategoryMininumMarkMapping).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CategoryMininumMarkMapping{config: cmmmq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cmmmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cmmmq.withLogData; query != nil {
		if err := cmmmq.loadLogData(ctx, query, nodes,
			func(n *CategoryMininumMarkMapping) { n.Edges.LogData = []*Logs{} },
			func(n *CategoryMininumMarkMapping, e *Logs) { n.Edges.LogData = append(n.Edges.LogData, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cmmmq *CategoryMininumMarkMappingQuery) loadLogData(ctx context.Context, query *LogsQuery, nodes []*CategoryMininumMarkMapping, init func(*CategoryMininumMarkMapping), assign func(*CategoryMininumMarkMapping, *Logs)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*CategoryMininumMarkMapping)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Logs(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(categorymininummarkmapping.LogDataColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.category_mininum_mark_mapping_log_data
		if fk == nil {
			return fmt.Errorf(`foreign-key "category_mininum_mark_mapping_log_data" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "category_mininum_mark_mapping_log_data" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (cmmmq *CategoryMininumMarkMappingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cmmmq.querySpec()
	_spec.Node.Columns = cmmmq.ctx.Fields
	if len(cmmmq.ctx.Fields) > 0 {
		_spec.Unique = cmmmq.ctx.Unique != nil && *cmmmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cmmmq.driver, _spec)
}

func (cmmmq *CategoryMininumMarkMappingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(categorymininummarkmapping.Table, categorymininummarkmapping.Columns, sqlgraph.NewFieldSpec(categorymininummarkmapping.FieldID, field.TypeInt64))
	_spec.From = cmmmq.sql
	if unique := cmmmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cmmmq.path != nil {
		_spec.Unique = true
	}
	if fields := cmmmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, categorymininummarkmapping.FieldID)
		for i := range fields {
			if fields[i] != categorymininummarkmapping.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cmmmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cmmmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cmmmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cmmmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cmmmq *CategoryMininumMarkMappingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cmmmq.driver.Dialect())
	t1 := builder.Table(categorymininummarkmapping.Table)
	columns := cmmmq.ctx.Fields
	if len(columns) == 0 {
		columns = categorymininummarkmapping.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cmmmq.sql != nil {
		selector = cmmmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cmmmq.ctx.Unique != nil && *cmmmq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cmmmq.predicates {
		p(selector)
	}
	for _, p := range cmmmq.order {
		p(selector)
	}
	if offset := cmmmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cmmmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CategoryMininumMarkMappingGroupBy is the group-by builder for CategoryMininumMarkMapping entities.
type CategoryMininumMarkMappingGroupBy struct {
	selector
	build *CategoryMininumMarkMappingQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cmmmgb *CategoryMininumMarkMappingGroupBy) Aggregate(fns ...AggregateFunc) *CategoryMininumMarkMappingGroupBy {
	cmmmgb.fns = append(cmmmgb.fns, fns...)
	return cmmmgb
}

// Scan applies the selector query and scans the result into the given value.
func (cmmmgb *CategoryMininumMarkMappingGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cmmmgb.build.ctx, "GroupBy")
	if err := cmmmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CategoryMininumMarkMappingQuery, *CategoryMininumMarkMappingGroupBy](ctx, cmmmgb.build, cmmmgb, cmmmgb.build.inters, v)
}

func (cmmmgb *CategoryMininumMarkMappingGroupBy) sqlScan(ctx context.Context, root *CategoryMininumMarkMappingQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cmmmgb.fns))
	for _, fn := range cmmmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cmmmgb.flds)+len(cmmmgb.fns))
		for _, f := range *cmmmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cmmmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cmmmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CategoryMininumMarkMappingSelect is the builder for selecting fields of CategoryMininumMarkMapping entities.
type CategoryMininumMarkMappingSelect struct {
	*CategoryMininumMarkMappingQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cmmms *CategoryMininumMarkMappingSelect) Aggregate(fns ...AggregateFunc) *CategoryMininumMarkMappingSelect {
	cmmms.fns = append(cmmms.fns, fns...)
	return cmmms
}

// Scan applies the selector query and scans the result into the given value.
func (cmmms *CategoryMininumMarkMappingSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cmmms.ctx, "Select")
	if err := cmmms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CategoryMininumMarkMappingQuery, *CategoryMininumMarkMappingSelect](ctx, cmmms.CategoryMininumMarkMappingQuery, cmmms, cmmms.inters, v)
}

func (cmmms *CategoryMininumMarkMappingSelect) sqlScan(ctx context.Context, root *CategoryMininumMarkMappingQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cmmms.fns))
	for _, fn := range cmmms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cmmms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cmmms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
