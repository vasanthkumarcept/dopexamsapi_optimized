// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"recruit/ent/center"
	"recruit/ent/disability"
	"recruit/ent/eligibilitymaster"
	"recruit/ent/examcalendar"
	"recruit/ent/exampapers"
	"recruit/ent/papertypes"
	"recruit/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ExamPapersQuery is the builder for querying ExamPapers entities.
type ExamPapersQuery struct {
	config
	ctx                      *QueryContext
	order                    []exampapers.OrderOption
	inters                   []Interceptor
	predicates               []predicate.ExamPapers
	withCenters              *CenterQuery
	withExampapersTypes      *PaperTypesQuery
	withPapersRef            *ExamCalendarQuery
	withExamPaperEligibility *EligibilityMasterQuery
	withDisRef               *DisabilityQuery
	withFKs                  bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ExamPapersQuery builder.
func (epq *ExamPapersQuery) Where(ps ...predicate.ExamPapers) *ExamPapersQuery {
	epq.predicates = append(epq.predicates, ps...)
	return epq
}

// Limit the number of records to be returned by this query.
func (epq *ExamPapersQuery) Limit(limit int) *ExamPapersQuery {
	epq.ctx.Limit = &limit
	return epq
}

// Offset to start from.
func (epq *ExamPapersQuery) Offset(offset int) *ExamPapersQuery {
	epq.ctx.Offset = &offset
	return epq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (epq *ExamPapersQuery) Unique(unique bool) *ExamPapersQuery {
	epq.ctx.Unique = &unique
	return epq
}

// Order specifies how the records should be ordered.
func (epq *ExamPapersQuery) Order(o ...exampapers.OrderOption) *ExamPapersQuery {
	epq.order = append(epq.order, o...)
	return epq
}

// QueryCenters chains the current query on the "centers" edge.
func (epq *ExamPapersQuery) QueryCenters() *CenterQuery {
	query := (&CenterClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exampapers.Table, exampapers.FieldID, selector),
			sqlgraph.To(center.Table, center.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exampapers.CentersTable, exampapers.CentersColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExampapersTypes chains the current query on the "exampapers_types" edge.
func (epq *ExamPapersQuery) QueryExampapersTypes() *PaperTypesQuery {
	query := (&PaperTypesClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exampapers.Table, exampapers.FieldID, selector),
			sqlgraph.To(papertypes.Table, papertypes.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exampapers.ExampapersTypesTable, exampapers.ExampapersTypesColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPapersRef chains the current query on the "papers_ref" edge.
func (epq *ExamPapersQuery) QueryPapersRef() *ExamCalendarQuery {
	query := (&ExamCalendarClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exampapers.Table, exampapers.FieldID, selector),
			sqlgraph.To(examcalendar.Table, examcalendar.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exampapers.PapersRefTable, exampapers.PapersRefColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryExamPaperEligibility chains the current query on the "ExamPaperEligibility" edge.
func (epq *ExamPapersQuery) QueryExamPaperEligibility() *EligibilityMasterQuery {
	query := (&EligibilityMasterClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exampapers.Table, exampapers.FieldID, selector),
			sqlgraph.To(eligibilitymaster.Table, eligibilitymaster.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exampapers.ExamPaperEligibilityTable, exampapers.ExamPaperEligibilityColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDisRef chains the current query on the "dis_ref" edge.
func (epq *ExamPapersQuery) QueryDisRef() *DisabilityQuery {
	query := (&DisabilityClient{config: epq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := epq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := epq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(exampapers.Table, exampapers.FieldID, selector),
			sqlgraph.To(disability.Table, disability.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, exampapers.DisRefTable, exampapers.DisRefColumn),
		)
		fromU = sqlgraph.SetNeighbors(epq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ExamPapers entity from the query.
// Returns a *NotFoundError when no ExamPapers was found.
func (epq *ExamPapersQuery) First(ctx context.Context) (*ExamPapers, error) {
	nodes, err := epq.Limit(1).All(setContextOp(ctx, epq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{exampapers.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (epq *ExamPapersQuery) FirstX(ctx context.Context) *ExamPapers {
	node, err := epq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ExamPapers ID from the query.
// Returns a *NotFoundError when no ExamPapers ID was found.
func (epq *ExamPapersQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = epq.Limit(1).IDs(setContextOp(ctx, epq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{exampapers.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (epq *ExamPapersQuery) FirstIDX(ctx context.Context) int32 {
	id, err := epq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ExamPapers entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ExamPapers entity is found.
// Returns a *NotFoundError when no ExamPapers entities are found.
func (epq *ExamPapersQuery) Only(ctx context.Context) (*ExamPapers, error) {
	nodes, err := epq.Limit(2).All(setContextOp(ctx, epq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{exampapers.Label}
	default:
		return nil, &NotSingularError{exampapers.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (epq *ExamPapersQuery) OnlyX(ctx context.Context) *ExamPapers {
	node, err := epq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ExamPapers ID in the query.
// Returns a *NotSingularError when more than one ExamPapers ID is found.
// Returns a *NotFoundError when no entities are found.
func (epq *ExamPapersQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = epq.Limit(2).IDs(setContextOp(ctx, epq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{exampapers.Label}
	default:
		err = &NotSingularError{exampapers.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (epq *ExamPapersQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := epq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ExamPapersSlice.
func (epq *ExamPapersQuery) All(ctx context.Context) ([]*ExamPapers, error) {
	ctx = setContextOp(ctx, epq.ctx, "All")
	if err := epq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ExamPapers, *ExamPapersQuery]()
	return withInterceptors[[]*ExamPapers](ctx, epq, qr, epq.inters)
}

// AllX is like All, but panics if an error occurs.
func (epq *ExamPapersQuery) AllX(ctx context.Context) []*ExamPapers {
	nodes, err := epq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ExamPapers IDs.
func (epq *ExamPapersQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if epq.ctx.Unique == nil && epq.path != nil {
		epq.Unique(true)
	}
	ctx = setContextOp(ctx, epq.ctx, "IDs")
	if err = epq.Select(exampapers.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (epq *ExamPapersQuery) IDsX(ctx context.Context) []int32 {
	ids, err := epq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (epq *ExamPapersQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, epq.ctx, "Count")
	if err := epq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, epq, querierCount[*ExamPapersQuery](), epq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (epq *ExamPapersQuery) CountX(ctx context.Context) int {
	count, err := epq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (epq *ExamPapersQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, epq.ctx, "Exist")
	switch _, err := epq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (epq *ExamPapersQuery) ExistX(ctx context.Context) bool {
	exist, err := epq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ExamPapersQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (epq *ExamPapersQuery) Clone() *ExamPapersQuery {
	if epq == nil {
		return nil
	}
	return &ExamPapersQuery{
		config:                   epq.config,
		ctx:                      epq.ctx.Clone(),
		order:                    append([]exampapers.OrderOption{}, epq.order...),
		inters:                   append([]Interceptor{}, epq.inters...),
		predicates:               append([]predicate.ExamPapers{}, epq.predicates...),
		withCenters:              epq.withCenters.Clone(),
		withExampapersTypes:      epq.withExampapersTypes.Clone(),
		withPapersRef:            epq.withPapersRef.Clone(),
		withExamPaperEligibility: epq.withExamPaperEligibility.Clone(),
		withDisRef:               epq.withDisRef.Clone(),
		// clone intermediate query.
		sql:  epq.sql.Clone(),
		path: epq.path,
	}
}

// WithCenters tells the query-builder to eager-load the nodes that are connected to
// the "centers" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPapersQuery) WithCenters(opts ...func(*CenterQuery)) *ExamPapersQuery {
	query := (&CenterClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withCenters = query
	return epq
}

// WithExampapersTypes tells the query-builder to eager-load the nodes that are connected to
// the "exampapers_types" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPapersQuery) WithExampapersTypes(opts ...func(*PaperTypesQuery)) *ExamPapersQuery {
	query := (&PaperTypesClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withExampapersTypes = query
	return epq
}

// WithPapersRef tells the query-builder to eager-load the nodes that are connected to
// the "papers_ref" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPapersQuery) WithPapersRef(opts ...func(*ExamCalendarQuery)) *ExamPapersQuery {
	query := (&ExamCalendarClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withPapersRef = query
	return epq
}

// WithExamPaperEligibility tells the query-builder to eager-load the nodes that are connected to
// the "ExamPaperEligibility" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPapersQuery) WithExamPaperEligibility(opts ...func(*EligibilityMasterQuery)) *ExamPapersQuery {
	query := (&EligibilityMasterClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withExamPaperEligibility = query
	return epq
}

// WithDisRef tells the query-builder to eager-load the nodes that are connected to
// the "dis_ref" edge. The optional arguments are used to configure the query builder of the edge.
func (epq *ExamPapersQuery) WithDisRef(opts ...func(*DisabilityQuery)) *ExamPapersQuery {
	query := (&DisabilityClient{config: epq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	epq.withDisRef = query
	return epq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PaperDescription string `json:"PaperDescription,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ExamPapers.Query().
//		GroupBy(exampapers.FieldPaperDescription).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (epq *ExamPapersQuery) GroupBy(field string, fields ...string) *ExamPapersGroupBy {
	epq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ExamPapersGroupBy{build: epq}
	grbuild.flds = &epq.ctx.Fields
	grbuild.label = exampapers.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PaperDescription string `json:"PaperDescription,omitempty"`
//	}
//
//	client.ExamPapers.Query().
//		Select(exampapers.FieldPaperDescription).
//		Scan(ctx, &v)
func (epq *ExamPapersQuery) Select(fields ...string) *ExamPapersSelect {
	epq.ctx.Fields = append(epq.ctx.Fields, fields...)
	sbuild := &ExamPapersSelect{ExamPapersQuery: epq}
	sbuild.label = exampapers.Label
	sbuild.flds, sbuild.scan = &epq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ExamPapersSelect configured with the given aggregations.
func (epq *ExamPapersQuery) Aggregate(fns ...AggregateFunc) *ExamPapersSelect {
	return epq.Select().Aggregate(fns...)
}

func (epq *ExamPapersQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range epq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, epq); err != nil {
				return err
			}
		}
	}
	for _, f := range epq.ctx.Fields {
		if !exampapers.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if epq.path != nil {
		prev, err := epq.path(ctx)
		if err != nil {
			return err
		}
		epq.sql = prev
	}
	return nil
}

func (epq *ExamPapersQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ExamPapers, error) {
	var (
		nodes       = []*ExamPapers{}
		withFKs     = epq.withFKs
		_spec       = epq.querySpec()
		loadedTypes = [5]bool{
			epq.withCenters != nil,
			epq.withExampapersTypes != nil,
			epq.withPapersRef != nil,
			epq.withExamPaperEligibility != nil,
			epq.withDisRef != nil,
		}
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, exampapers.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ExamPapers).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ExamPapers{config: epq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, epq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := epq.withCenters; query != nil {
		if err := epq.loadCenters(ctx, query, nodes,
			func(n *ExamPapers) { n.Edges.Centers = []*Center{} },
			func(n *ExamPapers, e *Center) { n.Edges.Centers = append(n.Edges.Centers, e) }); err != nil {
			return nil, err
		}
	}
	if query := epq.withExampapersTypes; query != nil {
		if err := epq.loadExampapersTypes(ctx, query, nodes,
			func(n *ExamPapers) { n.Edges.ExampapersTypes = []*PaperTypes{} },
			func(n *ExamPapers, e *PaperTypes) { n.Edges.ExampapersTypes = append(n.Edges.ExampapersTypes, e) }); err != nil {
			return nil, err
		}
	}
	if query := epq.withPapersRef; query != nil {
		if err := epq.loadPapersRef(ctx, query, nodes,
			func(n *ExamPapers) { n.Edges.PapersRef = []*ExamCalendar{} },
			func(n *ExamPapers, e *ExamCalendar) { n.Edges.PapersRef = append(n.Edges.PapersRef, e) }); err != nil {
			return nil, err
		}
	}
	if query := epq.withExamPaperEligibility; query != nil {
		if err := epq.loadExamPaperEligibility(ctx, query, nodes,
			func(n *ExamPapers) { n.Edges.ExamPaperEligibility = []*EligibilityMaster{} },
			func(n *ExamPapers, e *EligibilityMaster) {
				n.Edges.ExamPaperEligibility = append(n.Edges.ExamPaperEligibility, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := epq.withDisRef; query != nil {
		if err := epq.loadDisRef(ctx, query, nodes,
			func(n *ExamPapers) { n.Edges.DisRef = []*Disability{} },
			func(n *ExamPapers, e *Disability) { n.Edges.DisRef = append(n.Edges.DisRef, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (epq *ExamPapersQuery) loadCenters(ctx context.Context, query *CenterQuery, nodes []*ExamPapers, init func(*ExamPapers), assign func(*ExamPapers, *Center)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*ExamPapers)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Center(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exampapers.CentersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.exam_papers_centers
		if fk == nil {
			return fmt.Errorf(`foreign-key "exam_papers_centers" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "exam_papers_centers" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (epq *ExamPapersQuery) loadExampapersTypes(ctx context.Context, query *PaperTypesQuery, nodes []*ExamPapers, init func(*ExamPapers), assign func(*ExamPapers, *PaperTypes)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*ExamPapers)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(papertypes.FieldPaperCode)
	}
	query.Where(predicate.PaperTypes(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exampapers.ExampapersTypesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PaperCode
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "PaperCode" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (epq *ExamPapersQuery) loadPapersRef(ctx context.Context, query *ExamCalendarQuery, nodes []*ExamPapers, init func(*ExamPapers), assign func(*ExamPapers, *ExamCalendar)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*ExamPapers)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(examcalendar.FieldPaperCode)
	}
	query.Where(predicate.ExamCalendar(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exampapers.PapersRefColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PaperCode
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "PaperCode" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (epq *ExamPapersQuery) loadExamPaperEligibility(ctx context.Context, query *EligibilityMasterQuery, nodes []*ExamPapers, init func(*ExamPapers), assign func(*ExamPapers, *EligibilityMaster)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*ExamPapers)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(eligibilitymaster.FieldPaperCode)
	}
	query.Where(predicate.EligibilityMaster(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exampapers.ExamPaperEligibilityColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.PaperCode
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "PaperCode" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (epq *ExamPapersQuery) loadDisRef(ctx context.Context, query *DisabilityQuery, nodes []*ExamPapers, init func(*ExamPapers), assign func(*ExamPapers, *Disability)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int32]*ExamPapers)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Disability(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(exampapers.DisRefColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.exam_papers_dis_ref
		if fk == nil {
			return fmt.Errorf(`foreign-key "exam_papers_dis_ref" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "exam_papers_dis_ref" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (epq *ExamPapersQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := epq.querySpec()
	_spec.Node.Columns = epq.ctx.Fields
	if len(epq.ctx.Fields) > 0 {
		_spec.Unique = epq.ctx.Unique != nil && *epq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, epq.driver, _spec)
}

func (epq *ExamPapersQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(exampapers.Table, exampapers.Columns, sqlgraph.NewFieldSpec(exampapers.FieldID, field.TypeInt32))
	_spec.From = epq.sql
	if unique := epq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if epq.path != nil {
		_spec.Unique = true
	}
	if fields := epq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, exampapers.FieldID)
		for i := range fields {
			if fields[i] != exampapers.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := epq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := epq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := epq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := epq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (epq *ExamPapersQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(epq.driver.Dialect())
	t1 := builder.Table(exampapers.Table)
	columns := epq.ctx.Fields
	if len(columns) == 0 {
		columns = exampapers.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if epq.sql != nil {
		selector = epq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if epq.ctx.Unique != nil && *epq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range epq.predicates {
		p(selector)
	}
	for _, p := range epq.order {
		p(selector)
	}
	if offset := epq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := epq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ExamPapersGroupBy is the group-by builder for ExamPapers entities.
type ExamPapersGroupBy struct {
	selector
	build *ExamPapersQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (epgb *ExamPapersGroupBy) Aggregate(fns ...AggregateFunc) *ExamPapersGroupBy {
	epgb.fns = append(epgb.fns, fns...)
	return epgb
}

// Scan applies the selector query and scans the result into the given value.
func (epgb *ExamPapersGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, epgb.build.ctx, "GroupBy")
	if err := epgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamPapersQuery, *ExamPapersGroupBy](ctx, epgb.build, epgb, epgb.build.inters, v)
}

func (epgb *ExamPapersGroupBy) sqlScan(ctx context.Context, root *ExamPapersQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(epgb.fns))
	for _, fn := range epgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*epgb.flds)+len(epgb.fns))
		for _, f := range *epgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*epgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := epgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ExamPapersSelect is the builder for selecting fields of ExamPapers entities.
type ExamPapersSelect struct {
	*ExamPapersQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (eps *ExamPapersSelect) Aggregate(fns ...AggregateFunc) *ExamPapersSelect {
	eps.fns = append(eps.fns, fns...)
	return eps
}

// Scan applies the selector query and scans the result into the given value.
func (eps *ExamPapersSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eps.ctx, "Select")
	if err := eps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ExamPapersQuery, *ExamPapersSelect](ctx, eps.ExamPapersQuery, eps, eps.inters, v)
}

func (eps *ExamPapersSelect) sqlScan(ctx context.Context, root *ExamPapersQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(eps.fns))
	for _, fn := range eps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*eps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
