// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"recruit/ent/facilitymasters"
	"recruit/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FacilityMastersQuery is the builder for querying FacilityMasters entities.
type FacilityMastersQuery struct {
	config
	ctx        *QueryContext
	order      []facilitymasters.OrderOption
	inters     []Interceptor
	predicates []predicate.FacilityMasters
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FacilityMastersQuery builder.
func (fmq *FacilityMastersQuery) Where(ps ...predicate.FacilityMasters) *FacilityMastersQuery {
	fmq.predicates = append(fmq.predicates, ps...)
	return fmq
}

// Limit the number of records to be returned by this query.
func (fmq *FacilityMastersQuery) Limit(limit int) *FacilityMastersQuery {
	fmq.ctx.Limit = &limit
	return fmq
}

// Offset to start from.
func (fmq *FacilityMastersQuery) Offset(offset int) *FacilityMastersQuery {
	fmq.ctx.Offset = &offset
	return fmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fmq *FacilityMastersQuery) Unique(unique bool) *FacilityMastersQuery {
	fmq.ctx.Unique = &unique
	return fmq
}

// Order specifies how the records should be ordered.
func (fmq *FacilityMastersQuery) Order(o ...facilitymasters.OrderOption) *FacilityMastersQuery {
	fmq.order = append(fmq.order, o...)
	return fmq
}

// First returns the first FacilityMasters entity from the query.
// Returns a *NotFoundError when no FacilityMasters was found.
func (fmq *FacilityMastersQuery) First(ctx context.Context) (*FacilityMasters, error) {
	nodes, err := fmq.Limit(1).All(setContextOp(ctx, fmq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{facilitymasters.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fmq *FacilityMastersQuery) FirstX(ctx context.Context) *FacilityMasters {
	node, err := fmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FacilityMasters ID from the query.
// Returns a *NotFoundError when no FacilityMasters ID was found.
func (fmq *FacilityMastersQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = fmq.Limit(1).IDs(setContextOp(ctx, fmq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{facilitymasters.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fmq *FacilityMastersQuery) FirstIDX(ctx context.Context) int32 {
	id, err := fmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FacilityMasters entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FacilityMasters entity is found.
// Returns a *NotFoundError when no FacilityMasters entities are found.
func (fmq *FacilityMastersQuery) Only(ctx context.Context) (*FacilityMasters, error) {
	nodes, err := fmq.Limit(2).All(setContextOp(ctx, fmq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{facilitymasters.Label}
	default:
		return nil, &NotSingularError{facilitymasters.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fmq *FacilityMastersQuery) OnlyX(ctx context.Context) *FacilityMasters {
	node, err := fmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FacilityMasters ID in the query.
// Returns a *NotSingularError when more than one FacilityMasters ID is found.
// Returns a *NotFoundError when no entities are found.
func (fmq *FacilityMastersQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = fmq.Limit(2).IDs(setContextOp(ctx, fmq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{facilitymasters.Label}
	default:
		err = &NotSingularError{facilitymasters.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fmq *FacilityMastersQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := fmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FacilityMastersSlice.
func (fmq *FacilityMastersQuery) All(ctx context.Context) ([]*FacilityMasters, error) {
	ctx = setContextOp(ctx, fmq.ctx, "All")
	if err := fmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FacilityMasters, *FacilityMastersQuery]()
	return withInterceptors[[]*FacilityMasters](ctx, fmq, qr, fmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fmq *FacilityMastersQuery) AllX(ctx context.Context) []*FacilityMasters {
	nodes, err := fmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FacilityMasters IDs.
func (fmq *FacilityMastersQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if fmq.ctx.Unique == nil && fmq.path != nil {
		fmq.Unique(true)
	}
	ctx = setContextOp(ctx, fmq.ctx, "IDs")
	if err = fmq.Select(facilitymasters.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fmq *FacilityMastersQuery) IDsX(ctx context.Context) []int32 {
	ids, err := fmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fmq *FacilityMastersQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fmq.ctx, "Count")
	if err := fmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fmq, querierCount[*FacilityMastersQuery](), fmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fmq *FacilityMastersQuery) CountX(ctx context.Context) int {
	count, err := fmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fmq *FacilityMastersQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fmq.ctx, "Exist")
	switch _, err := fmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fmq *FacilityMastersQuery) ExistX(ctx context.Context) bool {
	exist, err := fmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FacilityMastersQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fmq *FacilityMastersQuery) Clone() *FacilityMastersQuery {
	if fmq == nil {
		return nil
	}
	return &FacilityMastersQuery{
		config:     fmq.config,
		ctx:        fmq.ctx.Clone(),
		order:      append([]facilitymasters.OrderOption{}, fmq.order...),
		inters:     append([]Interceptor{}, fmq.inters...),
		predicates: append([]predicate.FacilityMasters{}, fmq.predicates...),
		// clone intermediate query.
		sql:  fmq.sql.Clone(),
		path: fmq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FacilityID string `json:"FacilityID,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FacilityMasters.Query().
//		GroupBy(facilitymasters.FieldFacilityID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fmq *FacilityMastersQuery) GroupBy(field string, fields ...string) *FacilityMastersGroupBy {
	fmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FacilityMastersGroupBy{build: fmq}
	grbuild.flds = &fmq.ctx.Fields
	grbuild.label = facilitymasters.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FacilityID string `json:"FacilityID,omitempty"`
//	}
//
//	client.FacilityMasters.Query().
//		Select(facilitymasters.FieldFacilityID).
//		Scan(ctx, &v)
func (fmq *FacilityMastersQuery) Select(fields ...string) *FacilityMastersSelect {
	fmq.ctx.Fields = append(fmq.ctx.Fields, fields...)
	sbuild := &FacilityMastersSelect{FacilityMastersQuery: fmq}
	sbuild.label = facilitymasters.Label
	sbuild.flds, sbuild.scan = &fmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FacilityMastersSelect configured with the given aggregations.
func (fmq *FacilityMastersQuery) Aggregate(fns ...AggregateFunc) *FacilityMastersSelect {
	return fmq.Select().Aggregate(fns...)
}

func (fmq *FacilityMastersQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fmq); err != nil {
				return err
			}
		}
	}
	for _, f := range fmq.ctx.Fields {
		if !facilitymasters.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fmq.path != nil {
		prev, err := fmq.path(ctx)
		if err != nil {
			return err
		}
		fmq.sql = prev
	}
	return nil
}

func (fmq *FacilityMastersQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FacilityMasters, error) {
	var (
		nodes = []*FacilityMasters{}
		_spec = fmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FacilityMasters).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FacilityMasters{config: fmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fmq *FacilityMastersQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fmq.querySpec()
	_spec.Node.Columns = fmq.ctx.Fields
	if len(fmq.ctx.Fields) > 0 {
		_spec.Unique = fmq.ctx.Unique != nil && *fmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fmq.driver, _spec)
}

func (fmq *FacilityMastersQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(facilitymasters.Table, facilitymasters.Columns, sqlgraph.NewFieldSpec(facilitymasters.FieldID, field.TypeInt32))
	_spec.From = fmq.sql
	if unique := fmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fmq.path != nil {
		_spec.Unique = true
	}
	if fields := fmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, facilitymasters.FieldID)
		for i := range fields {
			if fields[i] != facilitymasters.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fmq *FacilityMastersQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fmq.driver.Dialect())
	t1 := builder.Table(facilitymasters.Table)
	columns := fmq.ctx.Fields
	if len(columns) == 0 {
		columns = facilitymasters.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fmq.sql != nil {
		selector = fmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fmq.ctx.Unique != nil && *fmq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fmq.predicates {
		p(selector)
	}
	for _, p := range fmq.order {
		p(selector)
	}
	if offset := fmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FacilityMastersGroupBy is the group-by builder for FacilityMasters entities.
type FacilityMastersGroupBy struct {
	selector
	build *FacilityMastersQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fmgb *FacilityMastersGroupBy) Aggregate(fns ...AggregateFunc) *FacilityMastersGroupBy {
	fmgb.fns = append(fmgb.fns, fns...)
	return fmgb
}

// Scan applies the selector query and scans the result into the given value.
func (fmgb *FacilityMastersGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fmgb.build.ctx, "GroupBy")
	if err := fmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FacilityMastersQuery, *FacilityMastersGroupBy](ctx, fmgb.build, fmgb, fmgb.build.inters, v)
}

func (fmgb *FacilityMastersGroupBy) sqlScan(ctx context.Context, root *FacilityMastersQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fmgb.fns))
	for _, fn := range fmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fmgb.flds)+len(fmgb.fns))
		for _, f := range *fmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FacilityMastersSelect is the builder for selecting fields of FacilityMasters entities.
type FacilityMastersSelect struct {
	*FacilityMastersQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fms *FacilityMastersSelect) Aggregate(fns ...AggregateFunc) *FacilityMastersSelect {
	fms.fns = append(fms.fns, fns...)
	return fms
}

// Scan applies the selector query and scans the result into the given value.
func (fms *FacilityMastersSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fms.ctx, "Select")
	if err := fms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FacilityMastersQuery, *FacilityMastersSelect](ctx, fms.FacilityMastersQuery, fms, fms.inters, v)
}

func (fms *FacilityMastersSelect) sqlScan(ctx context.Context, root *FacilityMastersQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fms.fns))
	for _, fn := range fms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
