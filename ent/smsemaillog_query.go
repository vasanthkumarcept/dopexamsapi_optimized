// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"recruit/ent/predicate"
	"recruit/ent/smsemaillog"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SmsEmailLogQuery is the builder for querying SmsEmailLog entities.
type SmsEmailLogQuery struct {
	config
	ctx        *QueryContext
	order      []smsemaillog.OrderOption
	inters     []Interceptor
	predicates []predicate.SmsEmailLog
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SmsEmailLogQuery builder.
func (selq *SmsEmailLogQuery) Where(ps ...predicate.SmsEmailLog) *SmsEmailLogQuery {
	selq.predicates = append(selq.predicates, ps...)
	return selq
}

// Limit the number of records to be returned by this query.
func (selq *SmsEmailLogQuery) Limit(limit int) *SmsEmailLogQuery {
	selq.ctx.Limit = &limit
	return selq
}

// Offset to start from.
func (selq *SmsEmailLogQuery) Offset(offset int) *SmsEmailLogQuery {
	selq.ctx.Offset = &offset
	return selq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (selq *SmsEmailLogQuery) Unique(unique bool) *SmsEmailLogQuery {
	selq.ctx.Unique = &unique
	return selq
}

// Order specifies how the records should be ordered.
func (selq *SmsEmailLogQuery) Order(o ...smsemaillog.OrderOption) *SmsEmailLogQuery {
	selq.order = append(selq.order, o...)
	return selq
}

// First returns the first SmsEmailLog entity from the query.
// Returns a *NotFoundError when no SmsEmailLog was found.
func (selq *SmsEmailLogQuery) First(ctx context.Context) (*SmsEmailLog, error) {
	nodes, err := selq.Limit(1).All(setContextOp(ctx, selq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{smsemaillog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (selq *SmsEmailLogQuery) FirstX(ctx context.Context) *SmsEmailLog {
	node, err := selq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SmsEmailLog ID from the query.
// Returns a *NotFoundError when no SmsEmailLog ID was found.
func (selq *SmsEmailLogQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = selq.Limit(1).IDs(setContextOp(ctx, selq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{smsemaillog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (selq *SmsEmailLogQuery) FirstIDX(ctx context.Context) int64 {
	id, err := selq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SmsEmailLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SmsEmailLog entity is found.
// Returns a *NotFoundError when no SmsEmailLog entities are found.
func (selq *SmsEmailLogQuery) Only(ctx context.Context) (*SmsEmailLog, error) {
	nodes, err := selq.Limit(2).All(setContextOp(ctx, selq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{smsemaillog.Label}
	default:
		return nil, &NotSingularError{smsemaillog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (selq *SmsEmailLogQuery) OnlyX(ctx context.Context) *SmsEmailLog {
	node, err := selq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SmsEmailLog ID in the query.
// Returns a *NotSingularError when more than one SmsEmailLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (selq *SmsEmailLogQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = selq.Limit(2).IDs(setContextOp(ctx, selq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{smsemaillog.Label}
	default:
		err = &NotSingularError{smsemaillog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (selq *SmsEmailLogQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := selq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SmsEmailLogs.
func (selq *SmsEmailLogQuery) All(ctx context.Context) ([]*SmsEmailLog, error) {
	ctx = setContextOp(ctx, selq.ctx, "All")
	if err := selq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SmsEmailLog, *SmsEmailLogQuery]()
	return withInterceptors[[]*SmsEmailLog](ctx, selq, qr, selq.inters)
}

// AllX is like All, but panics if an error occurs.
func (selq *SmsEmailLogQuery) AllX(ctx context.Context) []*SmsEmailLog {
	nodes, err := selq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SmsEmailLog IDs.
func (selq *SmsEmailLogQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if selq.ctx.Unique == nil && selq.path != nil {
		selq.Unique(true)
	}
	ctx = setContextOp(ctx, selq.ctx, "IDs")
	if err = selq.Select(smsemaillog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (selq *SmsEmailLogQuery) IDsX(ctx context.Context) []int64 {
	ids, err := selq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (selq *SmsEmailLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, selq.ctx, "Count")
	if err := selq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, selq, querierCount[*SmsEmailLogQuery](), selq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (selq *SmsEmailLogQuery) CountX(ctx context.Context) int {
	count, err := selq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (selq *SmsEmailLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, selq.ctx, "Exist")
	switch _, err := selq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (selq *SmsEmailLogQuery) ExistX(ctx context.Context) bool {
	exist, err := selq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SmsEmailLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (selq *SmsEmailLogQuery) Clone() *SmsEmailLogQuery {
	if selq == nil {
		return nil
	}
	return &SmsEmailLogQuery{
		config:     selq.config,
		ctx:        selq.ctx.Clone(),
		order:      append([]smsemaillog.OrderOption{}, selq.order...),
		inters:     append([]Interceptor{}, selq.inters...),
		predicates: append([]predicate.SmsEmailLog{}, selq.predicates...),
		// clone intermediate query.
		sql:  selq.sql.Clone(),
		path: selq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Type string `json:"Type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SmsEmailLog.Query().
//		GroupBy(smsemaillog.FieldType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (selq *SmsEmailLogQuery) GroupBy(field string, fields ...string) *SmsEmailLogGroupBy {
	selq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SmsEmailLogGroupBy{build: selq}
	grbuild.flds = &selq.ctx.Fields
	grbuild.label = smsemaillog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Type string `json:"Type,omitempty"`
//	}
//
//	client.SmsEmailLog.Query().
//		Select(smsemaillog.FieldType).
//		Scan(ctx, &v)
func (selq *SmsEmailLogQuery) Select(fields ...string) *SmsEmailLogSelect {
	selq.ctx.Fields = append(selq.ctx.Fields, fields...)
	sbuild := &SmsEmailLogSelect{SmsEmailLogQuery: selq}
	sbuild.label = smsemaillog.Label
	sbuild.flds, sbuild.scan = &selq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SmsEmailLogSelect configured with the given aggregations.
func (selq *SmsEmailLogQuery) Aggregate(fns ...AggregateFunc) *SmsEmailLogSelect {
	return selq.Select().Aggregate(fns...)
}

func (selq *SmsEmailLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range selq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, selq); err != nil {
				return err
			}
		}
	}
	for _, f := range selq.ctx.Fields {
		if !smsemaillog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if selq.path != nil {
		prev, err := selq.path(ctx)
		if err != nil {
			return err
		}
		selq.sql = prev
	}
	return nil
}

func (selq *SmsEmailLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SmsEmailLog, error) {
	var (
		nodes = []*SmsEmailLog{}
		_spec = selq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SmsEmailLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SmsEmailLog{config: selq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, selq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (selq *SmsEmailLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := selq.querySpec()
	_spec.Node.Columns = selq.ctx.Fields
	if len(selq.ctx.Fields) > 0 {
		_spec.Unique = selq.ctx.Unique != nil && *selq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, selq.driver, _spec)
}

func (selq *SmsEmailLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(smsemaillog.Table, smsemaillog.Columns, sqlgraph.NewFieldSpec(smsemaillog.FieldID, field.TypeInt64))
	_spec.From = selq.sql
	if unique := selq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if selq.path != nil {
		_spec.Unique = true
	}
	if fields := selq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, smsemaillog.FieldID)
		for i := range fields {
			if fields[i] != smsemaillog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := selq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := selq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := selq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := selq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (selq *SmsEmailLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(selq.driver.Dialect())
	t1 := builder.Table(smsemaillog.Table)
	columns := selq.ctx.Fields
	if len(columns) == 0 {
		columns = smsemaillog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if selq.sql != nil {
		selector = selq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if selq.ctx.Unique != nil && *selq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range selq.predicates {
		p(selector)
	}
	for _, p := range selq.order {
		p(selector)
	}
	if offset := selq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := selq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SmsEmailLogGroupBy is the group-by builder for SmsEmailLog entities.
type SmsEmailLogGroupBy struct {
	selector
	build *SmsEmailLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (selgb *SmsEmailLogGroupBy) Aggregate(fns ...AggregateFunc) *SmsEmailLogGroupBy {
	selgb.fns = append(selgb.fns, fns...)
	return selgb
}

// Scan applies the selector query and scans the result into the given value.
func (selgb *SmsEmailLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, selgb.build.ctx, "GroupBy")
	if err := selgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SmsEmailLogQuery, *SmsEmailLogGroupBy](ctx, selgb.build, selgb, selgb.build.inters, v)
}

func (selgb *SmsEmailLogGroupBy) sqlScan(ctx context.Context, root *SmsEmailLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(selgb.fns))
	for _, fn := range selgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*selgb.flds)+len(selgb.fns))
		for _, f := range *selgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*selgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := selgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SmsEmailLogSelect is the builder for selecting fields of SmsEmailLog entities.
type SmsEmailLogSelect struct {
	*SmsEmailLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sels *SmsEmailLogSelect) Aggregate(fns ...AggregateFunc) *SmsEmailLogSelect {
	sels.fns = append(sels.fns, fns...)
	return sels
}

// Scan applies the selector query and scans the result into the given value.
func (sels *SmsEmailLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sels.ctx, "Select")
	if err := sels.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SmsEmailLogQuery, *SmsEmailLogSelect](ctx, sels.SmsEmailLogQuery, sels, sels.inters, v)
}

func (sels *SmsEmailLogSelect) sqlScan(ctx context.Context, root *SmsEmailLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sels.fns))
	for _, fn := range sels.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sels.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sels.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
