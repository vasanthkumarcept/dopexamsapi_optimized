// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"recruit/ent/predicate"
	"recruit/ent/reversal_application_ip"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ReversalApplicationIPQuery is the builder for querying Reversal_Application_IP entities.
type ReversalApplicationIPQuery struct {
	config
	ctx        *QueryContext
	order      []reversal_application_ip.OrderOption
	inters     []Interceptor
	predicates []predicate.Reversal_Application_IP
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ReversalApplicationIPQuery builder.
func (raiq *ReversalApplicationIPQuery) Where(ps ...predicate.Reversal_Application_IP) *ReversalApplicationIPQuery {
	raiq.predicates = append(raiq.predicates, ps...)
	return raiq
}

// Limit the number of records to be returned by this query.
func (raiq *ReversalApplicationIPQuery) Limit(limit int) *ReversalApplicationIPQuery {
	raiq.ctx.Limit = &limit
	return raiq
}

// Offset to start from.
func (raiq *ReversalApplicationIPQuery) Offset(offset int) *ReversalApplicationIPQuery {
	raiq.ctx.Offset = &offset
	return raiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (raiq *ReversalApplicationIPQuery) Unique(unique bool) *ReversalApplicationIPQuery {
	raiq.ctx.Unique = &unique
	return raiq
}

// Order specifies how the records should be ordered.
func (raiq *ReversalApplicationIPQuery) Order(o ...reversal_application_ip.OrderOption) *ReversalApplicationIPQuery {
	raiq.order = append(raiq.order, o...)
	return raiq
}

// First returns the first Reversal_Application_IP entity from the query.
// Returns a *NotFoundError when no Reversal_Application_IP was found.
func (raiq *ReversalApplicationIPQuery) First(ctx context.Context) (*Reversal_Application_IP, error) {
	nodes, err := raiq.Limit(1).All(setContextOp(ctx, raiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{reversal_application_ip.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (raiq *ReversalApplicationIPQuery) FirstX(ctx context.Context) *Reversal_Application_IP {
	node, err := raiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Reversal_Application_IP ID from the query.
// Returns a *NotFoundError when no Reversal_Application_IP ID was found.
func (raiq *ReversalApplicationIPQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = raiq.Limit(1).IDs(setContextOp(ctx, raiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{reversal_application_ip.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (raiq *ReversalApplicationIPQuery) FirstIDX(ctx context.Context) int64 {
	id, err := raiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Reversal_Application_IP entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Reversal_Application_IP entity is found.
// Returns a *NotFoundError when no Reversal_Application_IP entities are found.
func (raiq *ReversalApplicationIPQuery) Only(ctx context.Context) (*Reversal_Application_IP, error) {
	nodes, err := raiq.Limit(2).All(setContextOp(ctx, raiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{reversal_application_ip.Label}
	default:
		return nil, &NotSingularError{reversal_application_ip.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (raiq *ReversalApplicationIPQuery) OnlyX(ctx context.Context) *Reversal_Application_IP {
	node, err := raiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Reversal_Application_IP ID in the query.
// Returns a *NotSingularError when more than one Reversal_Application_IP ID is found.
// Returns a *NotFoundError when no entities are found.
func (raiq *ReversalApplicationIPQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = raiq.Limit(2).IDs(setContextOp(ctx, raiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{reversal_application_ip.Label}
	default:
		err = &NotSingularError{reversal_application_ip.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (raiq *ReversalApplicationIPQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := raiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Reversal_Application_IPs.
func (raiq *ReversalApplicationIPQuery) All(ctx context.Context) ([]*Reversal_Application_IP, error) {
	ctx = setContextOp(ctx, raiq.ctx, "All")
	if err := raiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Reversal_Application_IP, *ReversalApplicationIPQuery]()
	return withInterceptors[[]*Reversal_Application_IP](ctx, raiq, qr, raiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (raiq *ReversalApplicationIPQuery) AllX(ctx context.Context) []*Reversal_Application_IP {
	nodes, err := raiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Reversal_Application_IP IDs.
func (raiq *ReversalApplicationIPQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if raiq.ctx.Unique == nil && raiq.path != nil {
		raiq.Unique(true)
	}
	ctx = setContextOp(ctx, raiq.ctx, "IDs")
	if err = raiq.Select(reversal_application_ip.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (raiq *ReversalApplicationIPQuery) IDsX(ctx context.Context) []int64 {
	ids, err := raiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (raiq *ReversalApplicationIPQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, raiq.ctx, "Count")
	if err := raiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, raiq, querierCount[*ReversalApplicationIPQuery](), raiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (raiq *ReversalApplicationIPQuery) CountX(ctx context.Context) int {
	count, err := raiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (raiq *ReversalApplicationIPQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, raiq.ctx, "Exist")
	switch _, err := raiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (raiq *ReversalApplicationIPQuery) ExistX(ctx context.Context) bool {
	exist, err := raiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ReversalApplicationIPQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (raiq *ReversalApplicationIPQuery) Clone() *ReversalApplicationIPQuery {
	if raiq == nil {
		return nil
	}
	return &ReversalApplicationIPQuery{
		config:     raiq.config,
		ctx:        raiq.ctx.Clone(),
		order:      append([]reversal_application_ip.OrderOption{}, raiq.order...),
		inters:     append([]Interceptor{}, raiq.inters...),
		predicates: append([]predicate.Reversal_Application_IP{}, raiq.predicates...),
		// clone intermediate query.
		sql:  raiq.sql.Clone(),
		path: raiq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EmployeeID int64 `json:"EmployeeID,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ReversalApplicationIP.Query().
//		GroupBy(reversal_application_ip.FieldEmployeeID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (raiq *ReversalApplicationIPQuery) GroupBy(field string, fields ...string) *ReversalApplicationIPGroupBy {
	raiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ReversalApplicationIPGroupBy{build: raiq}
	grbuild.flds = &raiq.ctx.Fields
	grbuild.label = reversal_application_ip.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EmployeeID int64 `json:"EmployeeID,omitempty"`
//	}
//
//	client.ReversalApplicationIP.Query().
//		Select(reversal_application_ip.FieldEmployeeID).
//		Scan(ctx, &v)
func (raiq *ReversalApplicationIPQuery) Select(fields ...string) *ReversalApplicationIPSelect {
	raiq.ctx.Fields = append(raiq.ctx.Fields, fields...)
	sbuild := &ReversalApplicationIPSelect{ReversalApplicationIPQuery: raiq}
	sbuild.label = reversal_application_ip.Label
	sbuild.flds, sbuild.scan = &raiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ReversalApplicationIPSelect configured with the given aggregations.
func (raiq *ReversalApplicationIPQuery) Aggregate(fns ...AggregateFunc) *ReversalApplicationIPSelect {
	return raiq.Select().Aggregate(fns...)
}

func (raiq *ReversalApplicationIPQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range raiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, raiq); err != nil {
				return err
			}
		}
	}
	for _, f := range raiq.ctx.Fields {
		if !reversal_application_ip.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if raiq.path != nil {
		prev, err := raiq.path(ctx)
		if err != nil {
			return err
		}
		raiq.sql = prev
	}
	return nil
}

func (raiq *ReversalApplicationIPQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Reversal_Application_IP, error) {
	var (
		nodes = []*Reversal_Application_IP{}
		_spec = raiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Reversal_Application_IP).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Reversal_Application_IP{config: raiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, raiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (raiq *ReversalApplicationIPQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := raiq.querySpec()
	_spec.Node.Columns = raiq.ctx.Fields
	if len(raiq.ctx.Fields) > 0 {
		_spec.Unique = raiq.ctx.Unique != nil && *raiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, raiq.driver, _spec)
}

func (raiq *ReversalApplicationIPQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(reversal_application_ip.Table, reversal_application_ip.Columns, sqlgraph.NewFieldSpec(reversal_application_ip.FieldID, field.TypeInt64))
	_spec.From = raiq.sql
	if unique := raiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if raiq.path != nil {
		_spec.Unique = true
	}
	if fields := raiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, reversal_application_ip.FieldID)
		for i := range fields {
			if fields[i] != reversal_application_ip.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := raiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := raiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := raiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := raiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (raiq *ReversalApplicationIPQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(raiq.driver.Dialect())
	t1 := builder.Table(reversal_application_ip.Table)
	columns := raiq.ctx.Fields
	if len(columns) == 0 {
		columns = reversal_application_ip.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if raiq.sql != nil {
		selector = raiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if raiq.ctx.Unique != nil && *raiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range raiq.predicates {
		p(selector)
	}
	for _, p := range raiq.order {
		p(selector)
	}
	if offset := raiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := raiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ReversalApplicationIPGroupBy is the group-by builder for Reversal_Application_IP entities.
type ReversalApplicationIPGroupBy struct {
	selector
	build *ReversalApplicationIPQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (raigb *ReversalApplicationIPGroupBy) Aggregate(fns ...AggregateFunc) *ReversalApplicationIPGroupBy {
	raigb.fns = append(raigb.fns, fns...)
	return raigb
}

// Scan applies the selector query and scans the result into the given value.
func (raigb *ReversalApplicationIPGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, raigb.build.ctx, "GroupBy")
	if err := raigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReversalApplicationIPQuery, *ReversalApplicationIPGroupBy](ctx, raigb.build, raigb, raigb.build.inters, v)
}

func (raigb *ReversalApplicationIPGroupBy) sqlScan(ctx context.Context, root *ReversalApplicationIPQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(raigb.fns))
	for _, fn := range raigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*raigb.flds)+len(raigb.fns))
		for _, f := range *raigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*raigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := raigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ReversalApplicationIPSelect is the builder for selecting fields of ReversalApplicationIP entities.
type ReversalApplicationIPSelect struct {
	*ReversalApplicationIPQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rais *ReversalApplicationIPSelect) Aggregate(fns ...AggregateFunc) *ReversalApplicationIPSelect {
	rais.fns = append(rais.fns, fns...)
	return rais
}

// Scan applies the selector query and scans the result into the given value.
func (rais *ReversalApplicationIPSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rais.ctx, "Select")
	if err := rais.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ReversalApplicationIPQuery, *ReversalApplicationIPSelect](ctx, rais.ReversalApplicationIPQuery, rais, rais.inters, v)
}

func (rais *ReversalApplicationIPSelect) sqlScan(ctx context.Context, root *ReversalApplicationIPQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rais.fns))
	for _, fn := range rais.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rais.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rais.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
